Epic: MENTAL-ALIGN-V2

## spec — 2026-09-02 — rev b10c114 + uncommitted `.project/active/mental-model-reviewer/spec.md` (2026-09-02 09:04)
Point (re-derived): The skill improves through owner judgment plus two feedback bodies in two tiers — shared starter shipped in the skill directory, project-local written first, a run reads both, promotion owner-initiated and targeting the shared feedback file while the instruction files stay the contract. No automated checks of any kind, because a noisy suite gets ignored; sequencing stays at two steps.   [source: `.project/concepts/mental-alignment-checkpoint.md` SC7, Key Concept 7, Non-Goals, Next-Stage Handoff settled list, Owner's Words 2026-08-19 and 2026-08-20; grade: owner]
Falsifier: The spec adds a check that runs without the owner asking or that gates a run, or moves where feedback lands, what ships, or what promotion targets, without naming the change as an owner-directed amendment of the specific concept item it touches.
Findings:
- spec-F1 [DON'T] Amends concept decision 7: the writer reads the project-local tier only (concept: "a run reads both") and rule promotion targets the instruction files (concept: promotion "targets the shared feedback file rather than the instruction files, which stay the contract"). Surfaced in Problem with the right direction; understated in one respect: the success criterion "promotion needs no rewriting" also retires the concept's reason for the guardrail (owner: agent feedback "needs a revision to be generalized and useful"), which the spec says is preserved. The owner-review step survives; the rationale does not, and the record should say so. — concept SC7, KC7, Handoff settled list, Owner's Words 2026-08-19 (owner) — disposition: BLOCK, pending owner confirmation cited against this ID
- spec-F2 [DON'T] Narrows the [OWNER] non-goal "no automated checks of any kind" by adding a reviewer agent that runs on every artifact without being asked. Surfaced in Problem with the right direction; the confirmation should knowingly cover two things the spec does not name: the notes reach the owner at every pause, which is the noise the non-goal exists to avoid ("even something like 'every section aligns' is gonna create more noise than it may be worth"), and the amended wording also legitimizes the coordinator's existing send-back gates from the 2026-08-26 quality-ownership change (`.project/active/mental-model-quality-ownership/change.md`), which are not recorded in the concept. — concept Non-Goals, Handoff "(2026-08-20) No automated checks of any kind", Owner's Words 2026-08-20 (owner) — disposition: BLOCK, pending owner confirmation cited against this ID
- spec-F3 [DON'T] Not surfaced: the concept's [OWNER] non-goal puts "sequencing beyond two steps" out of scope, and the reviewer adds a third automated pass per artifact (writer, then reviewer, then the writer amends) before the pause. The spec counts two amended decisions; this is a third. — concept Non-Goals; Owner's Words 2026-08-19 "[Prompt 1] -> [Agent] -> [Result 1] -> [Prompt 2] -> [Agent] -> [Result 2]" / "we can start with the latter and split later" (owner) — disposition: BLOCK, pending owner confirmation cited against this ID
- spec-F4 [DO]    Attribution is not carried forward: the concept says feedback stays attributable to the version it reviewed, and the epic's Item 4 requires project-local entries attributed to run and HTML version; the spec's required entry shape (pattern, Bad, Good, or a conditional technique) names no attribution, and promotion moves the entry out of the project-local file. State whether attribution survives the new shape and the move. — concept KC6 / "Snapshot" bet `[AGENT] (ratified by owner, 2026-08-09)`; epic MENTAL-ALIGN-V2 Item 4 In Scope (agent/ratified) — disposition: DISPOSE-and-proceed, pending
Gate: BLOCKED (spec-F1, spec-F2, spec-F3)
Resolution path: one later block citing spec-F1, spec-F2, spec-F3 with the owner's confirmation of the spec's Problem-section amendments (plus the third amendment in spec-F3); amend the concept's settled list under its "Superseded (owner-directed)" pattern and the epic's Item 4 non-goal and "promotion targets the shared file" success criterion; per product-lens §2 the disposition is "the product contract is intentionally changing", so file an owner-provenance ADR via `.project/scripts/adr.sh new` citing these IDs.

## spec — 2026-09-02 — rev b10c114 + uncommitted spec.md, after lens findings (spec author)
Resolves: spec-F4 — the spec now carries an [INHERITED] requirement that every feedback entry stays attributed to the run and artifact version it reviewed, in both tiers; the recording/promotion success criterion states that an example keeps the attribution as its source line through the move, and a rule is rewritten by the owner into the prompt. Disposed by the spec author; the finding's grade is agent/ratified, so no owner call is needed.
Surfaced: spec-F1, spec-F2, spec-F3 — the spec's Problem section now names all three concept items with the finding IDs, states the decision-7 rationale as replaced rather than preserved (F1), names the two consequences the confirmation must cover (F2), and adds the sequencing non-goal as the third touched decision (F3). Not resolved: owner confirmation pending.
Gate: BLOCKED (spec-F1, spec-F2, spec-F3)

## spec — 2026-09-02 — rev b10c114 + uncommitted spec.md, after spec-review (owner confirmation)
Resolves: spec-F1, spec-F2, spec-F3 — the owner confirmed the amendments (`.project/active/mental-model-reviewer/spec-review.md`, Resolutions L1-1 and L1-2): the prompt-versus-feedback split, the writer not reading shared feedback, the reviewer reading the prompt plus both feedback tiers, and an advisory reviewer pass on every artifact inside the existing synthesis or render step. Under L1-2 the reviewer's notes and the writer's decisions reach neither the owner nor the coordinator, so spec-F2's "noise at every pause" consequence no longer arises; the 2026-08-26 coordinator gates are recorded under the same narrowing. spec-F3 is resolved by the owner placing the pass inside the existing steps: the two-step shape stands. Recorded: ADR 0012 (owner provenance); concept Next-Stage Handoff "Superseded (2026-09-02, owner-directed)" block; epic Item 4 non-goal and promotion criterion annotated, Item 6 added.
Gate: CLEAR
