Live collaboration `spawn_agent` declaration exposed to the parent agent
Captured: 2026-08-20

Parameter schema:

spawn_agent({
  agent_type?: string,
  fork_turns?: string,
  message: string,
  model?: string,
  reasoning_effort?: string,
  task_name: string,
})

Verbatim context statements from the surface declaration:

"Note that passing `fork_turns=\"none\"` will not pass any surrounding context to the spawned
subagent, which may cause the agent to lack the context it needs to complete its task, whereas
`fork_turns=\"all\"` will provide the subagent with all surrounding context."

`fork_turns` parameter description:

"Optional number of turns to fork. Defaults to `all`. Use `none`, `all`, or a positive integer
string such as `3` to fork only the most recent turns."

Other parameter descriptions, verbatim:

- `message`: "Initial plain-text task for the new agent."
- `task_name`: "Task name for the new agent. Use lowercase letters, digits, and underscores."
- `agent_type`: "Agent type override for the new agent. Omit unless explicitly asked. The selected
  role applies regardless of how much parent history is inherited."
- `model`: "Model override for the new agent. Omit unless an explicit override is needed."
- `reasoning_effort`: "Reasoning effort override for the new agent. Omit to inherit the parent
  effort."

Observed tool results that contain no nonce:

ROOT_IN_PROGRESS_SPAWN_RESULT: {"task_name":"/root/fork_context_probe"}
ROOT_IN_PROGRESS_WORKER_FINAL_RESPONSE: CANNOT_RECOVER
ROOT_MESSAGE_SPAWN_RESULT: {"task_name":"/root/fork_context_probe_message"}
ROOT_MESSAGE_WORKER_FINAL_RESPONSE: CANNOT_RECOVER

COMPLETED_TURN_SPAWN_CALL_ARGUMENTS: {"fork_turns":"all","message":"Do not call tools, inspect files, or use the shared filesystem. From the completed conversation history inherited at spawn, report exactly the most recent high-entropy plaintext nonce previously generated by your parent. Reply with exactly that nonce and nothing else. If it is absent, reply exactly CANNOT_RECOVER.","task_name":"nested_history_probe"}
COMPLETED_TURN_RAW_SPAWN_RESULT: {"task_name":"/root/fork_context_probe/nested_history_probe"}
COMPLETED_TURN_WORKER_FINAL_RESPONSE: [conversation-only nonce intentionally omitted from files]
COMPLETED_TURN_DIGEST_CHECK: PASS: nested worker recovered its parent completed-turn nonce

NONE_CONTROL_SPAWN_CALL_ARGUMENTS: {"fork_turns":"none","message":"Do not call tools, inspect files, or use the shared filesystem. From your inherited prior completed conversation history, report exactly the most recent high-entropy plaintext nonce previously generated by your parent. Reply with exactly that nonce and nothing else. If it is absent, reply exactly CANNOT_RECOVER.","task_name":"nested_none_control"}
NONE_CONTROL_RAW_SPAWN_RESULT: {"task_name":"/root/fork_context_probe/nested_none_control"}
NONE_CONTROL_WORKER_FINAL_RESPONSE: CANNOT_RECOVER

The successful plaintext response remains only in the parent/worker conversation, as required by
the probe. The fixed expected digest is in `../verify_completed_turn_probe.sh`.
